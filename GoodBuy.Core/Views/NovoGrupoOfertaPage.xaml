<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GoodBuy.Views.NovoGrupoOfertaPage"
             HeightRequest="200">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="labelLogin" TargetType="Label">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="TextColor" Value="#4e6cab"/>
                <Setter Property="Margin" Value="0,0,0,0"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout Spacing="10">
        <StackLayout Spacing="0">
            <Label Text="Nome do grupo a ser criado" Style="{StaticResource labelLogin}" HorizontalOptions="StartAndExpand"/>
            <Entry Placeholder="Nome do grupo" Text="{Binding Name}"/>
        </StackLayout>
        <StackLayout Spacing="10">
            <Label Text="Grupo Secreto" Style="{StaticResource labelLogin}" HorizontalOptions="StartAndExpand"/>
            <StackLayout Orientation="Horizontal">
                <Label Text="Restrito"  HorizontalOptions="Start"/>
                <Switch IsToggled="{Binding Private}" />
            </StackLayout>
        </StackLayout>        
        <Button Text="Criar" Command="{Binding ProfileDeviceLoginCommand}" HorizontalOptions="FillAndExpand" BackgroundColor="#4e6cab" TextColor="#FFF"/>
    </StackLayout>
</ContentPage>